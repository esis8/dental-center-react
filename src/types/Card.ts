export interface IUser {
  id: number;
  image: string;
  name: string;
  email: string;
}

export interface IUserDetail {
  name: string;
  email: string;
  username: string;
  phone: string;
}

export interface FavoriteItem {
  id: number;
  name: string;
  email: string;
}